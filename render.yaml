services:
  # GLM-5 Chat Web Service (Docker)
- type: web
  name: glm-5-chat
  runtime: docker
  region: oregon
  plan: free
  branch: main
  dockerfilePath: ./Dockerfile
  dockerContext: .
  healthCheckPath: /health
