services:
  # GLM-5 Chat Web Service (Python)
- type: web
  name: glm-5-chat
  env: python
  region: oregon
  plan: free
  branch: main
  rootDir: .
  buildCommand: pip install -r requirements.txt
  startCommand: python glm_server.py
