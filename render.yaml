services:
  # GLM-5 Chat Web Service (Docker)
- type: web
  name: glm-5-chat
  runtime: docker
  region: oregon
  plan: free
  branch: main
  rootDir: .
  dockerfilePath: ./Dockerfile
  healthCheckPath: /health
  envVars:
  - key: PORT
    value: 10000
