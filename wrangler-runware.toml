name = "runware-image-worker"
main = "worker-runware.js"
compatibility_date = "2024-01-01"

# Worker configuration
workers_dev = true

# Optional: Add a custom domain later
# routes = [
#   { pattern = "runware.yourdomain.com", zone_name = "yourdomain.com" }
# ]

[vars]
API_VERSION = "1.0"
