name = "imgupscaler-worker"
main = "worker-imgupscaler.js"
compatibility_date = "2024-01-01"

# Worker configuration
workers_dev = true
route = ""

# Environment variables (if needed)
[vars]
WORKER_NAME = "ImgUpscaler Upload Worker"
VERSION = "1.0.0"

# Optional: Custom domain routing
# [[routes]]
# pattern = "upload.yourdomain.com/*"
# zone_name = "yourdomain.com"
