name = "mu-devs-image-worker"
main = "worker.js"
compatibility_date = "2024-01-01"

# Worker configuration
workers_dev = true

# Optional: Add a custom domain later
# routes = [
#   { pattern = "mu-devs.yourdomain.com", zone_name = "yourdomain.com" }
# ]

[vars]
API_VERSION = "1.0"
